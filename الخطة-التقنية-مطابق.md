# الخطة التقنية — مُطابِق
### منصة تحليل السيرة الذاتية ومطابقة الوظائف تلقائياً

---

## 1. الفكرة باختصار

تطبيق يقارن سيرة المستخدم الذاتية بإعلان وظيفة حقيقي، يوضّح نسبة التوافق والفجوات، ثم يقترح وظائف أخرى مناسبة — بدعم كامل للعربية واللهجات الخليجية. الجمهور الأساسي: خريجون وباحثون عن عمل في السعودية والخليج.

---

## 2. البنية العامة للنظام (Architecture)

```
[واجهة المستخدم]        [الخادم / API]           [طبقة الذكاء الاصطناعي]      [قواعد البيانات]
   React / Next.js  →   Node.js أو FastAPI  →   نموذج لغوي (Claude API)  →   PostgreSQL
   رفع PDF/Word         معالجة الملفات            + قواعد مطابقة مساعدة       تخزين المستخدمين
   عرض النتائج          استدعاء الذكاء الاصطناعي    استخراج الكيانات (NLP)      والسير والنتائج
                         تخزين النتائج
```

المبدأ الأساسي: الواجهة لا تتحدث مباشرة مع نموذج الذكاء الاصطناعي؛ كل الاستدعاءات تمر عبر الخادم الخاص بك حتى تتحكم بالتكلفة، تحمي مفتاح الـ API، وتخزّن النتائج.

---

## 3. المكونات الرئيسية

### أ) استخراج نص السيرة الذاتية
- ملفات PDF: مكتبة مثل `pdf-parse` (Node) أو `pdfplumber` (Python).
- ملفات Word: مكتبة `mammoth` أو `python-docx`.
- تحدٍّ متوقع: السير الذاتية المصمّمة بجداول أو أعمدة (تصدر نص العمود بترتيب خاطئ) — يحتاج تنظيف نص بعد الاستخراج.

### ب) طبقة التحليل والمطابقة (الجزء الأهم)
خياران يمكن الجمع بينهما:
1. **نموذج لغوي (LLM)** — يُرسل نص السيرة ونص الوظيفة معاً، ويُطلب منه إرجاع تحليل منظّم (نسبة توافق، مهارات متطابقة، مهارات ناقصة، توصيات) بصيغة JSON محددة. هذا هو الأسرع للبناء ويعطي جودة عالية فوراً، وهو ما استخدمناه في نموذج العرض الأولي.
2. **معالجة لغة طبيعية تقليدية (NLP)** — استخراج كلمات مفتاحية ومهارات من الطرفين ومقارنتها إحصائياً (مثل TF-IDF أو تشابه المتجهات/embeddings). أرخص على نطاق واسع وأسرع، لكن أقل دقة في فهم السياق.

**التوصية للمشروع كمشروع تخرج:** ابدأ بالخيار الأول (LLM) لأنه يعطي نتيجة مقنعة بسرعة، ثم إذا سمح الوقت أضف طبقة embeddings لتقليل التكلفة على النطاق الواسع.

### ج) مطابقة الوظائف تلقائياً
يحتاج مصدر بيانات وظائف حقيقي. خيارات:
- APIs جاهزة لبعض منصات التوظيف (تحقق من التوفر في السعودية تحديداً، فأغلبها مغطى بشكل أفضل في السوق الأمريكي/الأوروبي).
- بناء "زاحف" (scraper) بسيط لمواقع توظيف محلية — يحتاج مراجعة شروط الاستخدام لكل موقع قبل التنفيذ.
- الخيار الأسهل للـMVP: يرفع المستخدم إعلان وظيفة واحد يدوياً (نسخ ولصق أو رابط) بدل الاعتماد على قاعدة وظائف ضخمة من البداية.

### د) دعم اللغة العربية واللهجات الخليجية
- النماذج اللغوية الحديثة (مثل Claude) تتعامل مع العربية الفصحى وكثير من المصطلحات الدارجة في إعلانات التوظيف الخليجية بشكل جيد افتراضياً.
- لتحسين الدقة: أضف قاموس مرادفات محلي (مثل "خبرة عملية" = "Experience"، "تخصص" = "Major") يُرسل كسياق إضافي مع كل طلب تحليل.

### هـ) قاعدة البيانات
جداول أساسية مقترحة:
- `users` (المستخدم، بريده، تاريخ التسجيل)
- `resumes` (نص السيرة المستخرج، تاريخ الرفع، مرتبط بالمستخدم)
- `job_posts` (نص الوظيفة، المصدر، تاريخ الإضافة)
- `matches` (نتيجة كل مقارنة: نسبة التوافق، المهارات الناقصة، مرتبطة بسيرة ووظيفة)

---

## 4. خطة التنفيذ المرحلية (Roadmap)

| المرحلة | المدة التقريبية | الهدف |
|---|---|---|
| 1. نسخة أولية (MVP) | 2–3 أسابيع | رفع سيرة + لصق وظيفة واحدة → تحليل عبر LLM → عرض النتيجة (هذا ما تم بناؤه في الصفحة التجريبية) |
| 2. تخزين وحسابات المستخدمين | أسبوع | تسجيل دخول، حفظ السير والنتائج السابقة |
| 3. قاعدة وظائف حقيقية | 2–3 أسابيع | إضافة مصدر وظائف (يدوي أو زاحف بسيط) وتفعيل "المطابقة التلقائية" |
| 4. تحسين الدقة | مستمر | إضافة قاموس مرادفات عربي، تحسين استخراج PDF، اختبار على سير حقيقية متنوعة |
| 5. الإطلاق التجريبي | — | دعوة مجموعة صغيرة (قائمة الانتظار) للاختبار وجمع ملاحظات |

---

## 5. التقنيات المقترحة (Stack)

- **الواجهة الأمامية:** React أو Next.js (يدعم العربية RTL بسهولة).
- **الخادم:** Node.js (Express/Fastify) أو Python (FastAPI) — الأخير مناسب أكثر إذا احتجت لاحقاً معالجة NLP محلية.
- **الذكاء الاصطناعي:** Anthropic API (نموذج Claude) للتحليل والمطابقة.
- **قاعدة البيانات:** PostgreSQL (علائقية، مناسبة للبيانات المرتبطة مثل المستخدم/السيرة/الوظيفة).
- **الاستضافة:** أي منصة سحابية بسيطة للبداية (Vercel للواجهة، Railway أو Render للخادم وقاعدة البيانات) — كافية تماماً لمشروع تخرج ونسخة تجريبية أولى.

---

## 6. تحديات متوقعة يجب ذكرها في التقرير الأكاديمي

- **تنوع تنسيقات السيرة الذاتية:** بعض التصاميم (جداول، أعمدة) تُصعّب الاستخراج الدقيق للنص.
- **قلة مصادر بيانات وظائف مفتوحة في السوق السعودي/الخليجي:** قد تحتاج حلاً يدوياً أو شراكة مع منصة توظيف محلية لاحقاً.
- **خصوصية البيانات:** السير الذاتية تحتوي معلومات شخصية حساسة (اسم، رقم هاتف، أحياناً صورة) — يجب تشفير التخزين وتحديد سياسة واضحة لحذف البيانات بناءً على طلب المستخدم.
- **تكلفة استدعاءات النموذج اللغوي:** كل تحليل هو استدعاء API له تكلفة؛ عند التوسع يُنصح بإضافة طبقة تخزين مؤقت (caching) للنتائج المتكررة وربما نموذج NLP أرخص للفرز الأولي قبل استخدام LLM على الحالات الأدق.

---

## 7. ما الذي تم بناؤه فعلياً حتى الآن

- صفحة هبوط كاملة بلغتين (عربي/إنجليزي) مع تبديل فوري للاتجاه والخط.
- نموذج تجريبي فعلي: يقارن نص سيرة ذاتية مع وصف وظيفة عبر استدعاء حقيقي لنموذج لغوي، ويعرض نسبة التوافق والمهارات المتطابقة والناقصة وتوصيات.
- نموذج تسجيل بريد إلكتروني فعلي (قائمة انتظار) يخزّن البيانات فعلياً.

هذا يمثل تقريباً "المرحلة 1" من خطة التنفيذ أعلاه، وهو أساس قوي جداً لعرضه كنموذج أولي (Proof of Concept) في مشروع التخرج.
